import {
    APGM2Program,
    Block,
    Fn,
    NumberExpr,
    VarDecl,
    VarName,
} from "../ast/ast.ts";

export function parseAPGM2Program(src: string): APGM2Program {
    throw new Error("TODO");
}
